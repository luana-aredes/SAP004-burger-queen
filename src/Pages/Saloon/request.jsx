import React from 'react';
{/*
React.useEffect(() => {
  const saveOrder = (clerk, client, table, ) => {
    return db.collection('request').add({
      clerk: ,
      client: ,
      table: ,

      hamburguer simples: []
  hamburguer: []


    })



    const createPost = (userId, texto, privacy, url, uidPhoto) => {
      return firebase.firestore().collection("posts").add({
        userId: userId,
        name: firebase.auth().currentUser.displayName,
        text: texto,
        photoUrl: url,
        photoUid: uidPhoto,
        likes: [],
        created: firebase.firestore.Timestamp.fromDate(new Date()).toDate().toLocaleString('pt-BR'),
        privacy: privacy,
        comments: [{
          userId: userId,
          created: firebase.firestore.Timestamp.fromDate(new Date()).toDate().toLocaleString('pt-BR'),
          comment: "",
          userName: ""
        }],




      }
  })
   */}